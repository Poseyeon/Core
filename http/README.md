# HTTP

These files contain manual tests for calling the REST APIs. I recommend using a Visual Studio Code extension such as REST Client to easily test the endpoints with these files.

In the JSON, you need to manually replace the placeholder 'string' with appropriate values. Please do not include these changes when submitting a pull request.